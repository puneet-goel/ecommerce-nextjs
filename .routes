SEO 
/_error (still pending) (static)
/404 (still pending) (static)

SEO Friendly + No protection needed + Auth Nav
/login (static)
/signup (static)
/reset-password (static)

SEO Friendly + No protection needed + Main Nav
/ (ISR, 3)
/search (ISR, 3)
/search/[productId] (server)
/order-summary ?_id = {} (server)
/cart (static)
/about (static)
/coupons (ISR, 10) 
/order-history (static + api)
/profile (static + api)

No SEO + Protection needed + Main Nav + Redux
(swr)
/profile/statistics 
/add-product 
/update-product/[productId]
/update-product
