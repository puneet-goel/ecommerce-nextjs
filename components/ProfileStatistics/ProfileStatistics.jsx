import styles from 'styles/profile_stats.module.scss';

const ProfileStatisticsComponent = () => {
	return <div className={styles.profile_wrapper}>Profile</div>;
};

export default ProfileStatisticsComponent;
